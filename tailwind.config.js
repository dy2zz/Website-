/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
  "./*.html",        // Scans all HTML files in the root
  "./js/**/*.js",    // Scans all JS files in js folder
],
  theme: {
    extend: {},
  },
  plugins: [],
}
